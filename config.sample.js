const config = {
    preUrl: 'http://localhost:1234',
    USERDATA: {
        email: '',
        pass: ''
    },

    /**
     * lq - alacsony minőség
     * mq - közepes minőség
     * hq - magas minőség
     */
    preferredQuality: 'hq'
};

module.exports = config;
